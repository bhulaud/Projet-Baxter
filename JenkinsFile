
    pipeline {
        triggers {
            cron('H 7 * * 1-6')
        }

        agent any
        
        stages {
            stage('Run Tests') {
                steps {
                    sh("""chmod +x test.sh;\
                          ./test.sh}
END
                        """)
                     step([$class: "TapPublisher", testResults: "**/*.tap"])
                }
            }

        }
        options { timestamps () }
    }
